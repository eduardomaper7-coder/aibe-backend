# Renombra este archivo a .env y completa los valores
GOOGLE_CLIENT_ID=xxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
# Debe coincidir con el Redirect URI autorizado en Google Cloud (backend):
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback
# A dónde enviamos al usuario tras login (tu front local):
FRONTEND_POST_LOGIN_URL=http://localhost:3000
# Token de firma para sesiones/cookies en caso de usarlas más adelante
SECRET_KEY=change_me
# Base de datos (sqlite para el MVP)
DATABASE_URL=sqlite+aiosqlite:///./aibe.db
# Scopes necesarios (principal para GBP):
GOOGLE_OAUTH_SCOPES=https://www.googleapis.com/auth/business.manage
