# --- Core web ---
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1

# --- DB async ---
SQLAlchemy==2.0.32
aiosqlite==0.20.0
greenlet==3.1.0

# --- OAuth / seguridad ---
Authlib==1.3.1
itsdangerous==2.2.0

# --- HTTP ---
httpx==0.27.0
requests==2.32.3   # bloquea versión estable

# --- Google (opcional si vas a llamar la API GBP desde backend) ---
google-auth==2.35.0
google-auth-oauthlib==1.2.0

# --- Starlette (ya viene con FastAPI, pero fijar evita conflictos) ---
starlette==0.37.2

# --- Otros útiles / recomendados ---
pydantic==2.9.2          # versión estable compatible
pydantic-settings==2.4.0 # si luego configuras la carga de .env con clases
